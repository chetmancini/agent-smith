# Agent Smith for Gemini CLI

This extension keeps Gemini on the existing shell-hook path instead of the newer native runtime.

Use it to:

- record session telemetry from Gemini CLI hooks
- roll metrics up into `~/.config/agent-smith/rollup.db`
- analyze recurring failures and tuning opportunities with the shared shell scripts

This extension is currently intended for a local `agent-smith` checkout.
